[project]
name = "image-exp"
version = "0.1.0"
description = "Qwen-Image-2.1 generation experiments with vLLM-Omni."
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
]

[tool.uv]
package = false
